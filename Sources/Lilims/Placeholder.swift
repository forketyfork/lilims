/// Top-level interface for the Lilims library.
public struct Lilims {
    /// Creates a new instance of the library.
    public init() {}

    /// Returns a friendly greeting string.
    public func greet() -> String { "Hello" }
}
